part of 'pages.dart';

class FormGeneratorPage extends StatelessWidget {
  const FormGeneratorPage({super.key});
  static const routeName = 'form_generator_page';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FormGenerator'),
      ),
    );
  }
}
