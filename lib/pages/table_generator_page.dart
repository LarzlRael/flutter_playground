part of 'pages.dart';

class TableGeneratorPage extends StatelessWidget {
  const TableGeneratorPage({super.key});
  static const routeName = 'table_generator_page';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TableGeneratorPage'),
      ),
    );
  }
}
